#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int r,c;
    scanf("%d%d",&r,&c);
    int arr[r][c];
    for(int row=0;row<r;row++){
        for(int col=0;col<c;col++){
            scanf("%d",&arr[row][col]);
        }
    }
    for(int row=0;row<r;row++){
        for(int col=0;col<c;col++){
            printf("%d ",arr[row][col]);
        }
        printf("\n");
    }
    return 0;
}
