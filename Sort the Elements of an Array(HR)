#include <stdio.h> 
#include <string.h> 
#include <math.h> 
#include <stdlib.h> 

int main() { 
    int n; 
    scanf("%d",&n); 
    int i,j; 
    int arr[n]; 
    for(i=0;i<n;i++){ 
        scanf("%d",&arr[i]); 
    } 

    //bubble sort logic
    for(i=0;i<n-1;i++){ 
        for(j=0;j<n-1-i;j++){ 
            if(arr[j]>arr[j+1]){ 
                int temp = arr[j]; 
                arr[j]=arr[j+1]; 
                arr[j+1]=temp; 
            } 
        } 
    } 

    //logic to display array after sorting
    for(int i=0; i<n;i++){ 
        printf("%d ",arr[i]); 
    } 
    printf("\n");

    return 0; 
}
